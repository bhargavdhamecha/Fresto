package com.fresto.constant;

/**
 * UserType is an enum representing different types of users in the application.
 * It currently includes ADMIN and USER types.
 */
public enum UserType {
    ADMIN("ADMIN"),
    USER("USER");

    UserType(String userType) {
    }
}
